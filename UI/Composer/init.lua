-- Test-runner entry point: `require("WhisperMessenger.UI.Composer")`
-- resolves to UI/Composer/init.lua via the "./?/init.lua" path.
-- In-game, the TOC loads each submodule file directly.
return require("WhisperMessenger.UI.Composer.Composer")
