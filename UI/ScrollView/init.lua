-- Compatibility shim: allows require("WhisperMessenger.UI.ScrollView") to
-- resolve to this directory. The facade lives in ScrollView.lua.
return require("WhisperMessenger.UI.ScrollView.ScrollView")
