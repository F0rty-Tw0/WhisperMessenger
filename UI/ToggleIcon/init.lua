-- Compatibility shim: allows require("WhisperMessenger.UI.ToggleIcon") to
-- resolve to this directory. The facade lives in ToggleIcon.lua.
return require("WhisperMessenger.UI.ToggleIcon.ToggleIcon")
